services:
  web:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
    command: sh -c "pip install --quiet -r requirements.txt && python app.py"
